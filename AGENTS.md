# Josh Pi Subagents

This is a minimalist plugin for the pi agent harness that allows pi to delegate tasks to subagents that have been defined in Markdown files.

## Dependencies

- `yaml` - for parsing YAML frontmatter in user-defined custom agent Markdown files.
